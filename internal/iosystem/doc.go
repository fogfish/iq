// Package iosystem provides universal I/O abstractions for processing "documents"
// through pipelines. It unifies input sources and makes them extensible
// (stdin, files, directories, S3, HTTP) and output destinations while
// maintaining stream processing capabilities (e.g. chunking, merging).
package iosystem
