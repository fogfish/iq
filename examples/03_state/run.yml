name: state
jobs:
  main:
    steps:
      - name: extract
        uses: prompts/extract.md
        output: text                # Store as {{.steps.text}}
      
      - name: transform
        uses: prompts/transform.md
        output: specs               # Store as {{.steps.specs}}
      
      - name: summarize
        uses: prompts/summarize.md
        output: summary             # Store as {{.steps.summary}}
